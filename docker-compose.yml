version: "3.8"

services:
  vibe_arch:
    image: structurizr/lite:latest
    container_name: vibe-structurizr
    ports:
      - "8080:8080"
    volumes:
      - ${HOST_PROJECT_ROOT:-.}/structurizr:/usr/local/structurizr
    environment:
      - STRUCTURIZR_WORKSPACE=workspace